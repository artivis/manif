codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "70...100"

comment:
  layout: "header, diff"
  behavior: default
  require_changes: false

ignore:
  - "external"
  - "test"
